diff --git a/deps/ProximalOperators/Project.toml b/deps/ProximalOperators/Project.toml
index 6be4a3c..bbe5076 100644
--- a/deps/ProximalOperators/Project.toml
+++ b/deps/ProximalOperators/Project.toml
@@ -2,9 +2,6 @@ name = "ProximalOperators"
 uuid = "a725b495-10eb-56fe-b38b-717eba820537"
 version = "0.17.0"
 
-[workspace]
-projects = ["docs", "test", "benchmark"]
-
 [deps]
 IterativeSolvers = "42fd0dbc-a981-5370-80f2-aaf504508153"
 LinearAlgebra = "37e2e46d-f89d-539d-b4ee-838fcccc9c8e"
@@ -17,14 +14,9 @@ SuiteSparse = "4607b0f0-06f3-5cda-b6b1-a6196a1729e9"
 TSVD = "9449cd9e-2762-5aa3-a617-5413e99d722e"
 
 [weakdeps]
-GPUArrays = "0c68f7d7-f131-5f86-a1c3-88cf8149b2d7"
-KernelAbstractions = "63c18a36-062a-441e-b654-da1e3ab1ce7c"
-OSQP = "ab2f91bb-94b4-55e3-9ba0-7f65df51de79"
 RecursiveArrayTools = "731186ca-8d62-57ce-b412-fbd966d074cd"
 
 [extensions]
-GpuExt = ["GPUArrays", "KernelAbstractions"]
-ProximalOperatorsOSQPExt = "OSQP"
 RecursiveArrayToolsExt = "RecursiveArrayTools"
 
 [compat]
@@ -34,7 +26,6 @@ KernelAbstractions = "0.9"
 LinearAlgebra = "1.4"
 LoopVectorization = "0.12"
 NestedThreading = "0.1"
-OSQP = "0.3 - 0.8"
 Polyester = "0.7"
 ProximalCore = "0.2.0"
 RecursiveArrayTools = "2, 3"
diff --git a/deps/ProximalOperators/src/ProximalOperators.jl b/deps/ProximalOperators/src/ProximalOperators.jl
index c49f3b9..5ac9fbc 100644
--- a/deps/ProximalOperators/src/ProximalOperators.jl
+++ b/deps/ProximalOperators/src/ProximalOperators.jl
@@ -74,7 +74,6 @@ include("functions/indNonnegative.jl")
 include("functions/indRealNonnegative.jl")
 include("functions/indNonpositive.jl")
 include("functions/indPoint.jl")
-include("functions/indPolyhedral.jl")
 include("functions/indPSD.jl")
 include("functions/indSimplex.jl")
 include("functions/indSOC.jl")
@@ -120,6 +119,11 @@ include("calculus/sum.jl")
 include("calculus/pointwiseMinimum.jl")
 include("calculus/proximalAverage.jl")
 
+# `RecursiveArrayTools` is always available in `MriReconstructionToolbox` (unlike the standalone
+# ProximalOperators.jl package, where it is only a weakdep behind a package extension), so this
+# support is included unconditionally -- after the calculus rules it adds methods for.
+include("recursive_array_tools.jl")
+
 # Functions obtained from basic (as special cases or using calculus rules)
 
 include("functions/hingeLoss.jl")
diff --git a/deps/ProximalOperators/src/functions/indPolyhedral.jl a/deps/ProximalOperators/src/functions/indPolyhedral.jl
deleted file mode 100644
index 989db84..0000000
--- a/deps/ProximalOperators/src/functions/indPolyhedral.jl
+++ /dev/null
@@ -1,59 +0,0 @@
-export IndPolyhedral
-
-abstract type IndPolyhedral end
-
-is_convex(::Type{<:IndPolyhedral}) = true
-is_set_indicator(::Type{<:IndPolyhedral}) = true
-
-"""
-    IndPolyhedral([l,] A, [u, xmin, xmax])
-
-Return the indicator function of the polyhedral set:
-```math
-S = \\{ x : x_\\min \\leq x \\leq x_\\max, l \\leq Ax \\leq u \\}.
-```
-Matrix `A` is a mandatory argument; when any of the bounds is not provided,
-it is assumed to be (plus or minus) infinity.
-
-The default `solver=:osqp` backend is provided by a package extension: load it
-with `using OSQP` before constructing the object, otherwise an error is raised.
-"""
-function IndPolyhedral(args...; solver=:osqp)
-    if solver == :osqp
-        IndPolyhedralOSQP(args...)
-    else
-        error("unknown solver")
-    end
-end
-
-# IndPolyhedral: OSQP implementation
-#
-# The struct below is defined here so that the `solver=:osqp` dispatch above can
-# refer to it, but everything that actually needs OSQP -- the constructors,
-# `prox!`, the function evaluation and `prox_naive` -- lives in the package
-# extension `ext/ProximalOperatorsOSQPExt.jl`, loaded once OSQP is available
-# (`using OSQP`). Without OSQP loaded, constructing an `IndPolyhedralOSQP`
-# (directly or via `IndPolyhedral(...; solver=:osqp)`) raises an informative
-# error.
-
-struct IndPolyhedralOSQP{R, M} <: IndPolyhedral
-    l::AbstractVector{R}
-    A::AbstractMatrix{R}
-    u::AbstractVector{R}
-    mod::M
-    # Explicit inner constructor: suppresses the auto-generated 4-positional-arg
-    # outer constructor, which would otherwise shadow the `(l, A, xmin, xmax)`
-    # constructor added by ProximalOperatorsOSQPExt.
-    IndPolyhedralOSQP{R, M}(l, A, u, mod) where {R, M} = new{R, M}(l, A, u, mod)
-end
-
-is_proximable(::Type{<:IndPolyhedralOSQP}) = false
-
-# The real constructors are added to this function by ProximalOperatorsOSQPExt;
-# this fallback only fires when OSQP is not loaded.
-function IndPolyhedralOSQP(args...; kwargs...)
-    error(
-        "IndPolyhedralOSQP requires the OSQP package: run `using OSQP` before " *
-        "constructing IndPolyhedralOSQP(...) or IndPolyhedral(...; solver=:osqp)."
-    )
-end
diff --git b/deps/ProximalOperators/src/recursive_array_tools.jl b/deps/ProximalOperators/src/recursive_array_tools.jl
new file mode 100644
index 0000000..50eb8df
--- /dev/null
+++ b/deps/ProximalOperators/src/recursive_array_tools.jl
@@ -0,0 +1,14 @@
+using RecursiveArrayTools: ArrayPartition
+
+(f::PrecomposedSlicedSeparableSum)(x::ArrayPartition) = f(x.x)
+prox!(y::ArrayPartition, f::PrecomposedSlicedSeparableSum, x::ArrayPartition, gamma) = prox!(y.x, f, x.x, gamma)
+
+(g::SeparableSum)(xs::ArrayPartition) = g(xs.x)
+prox!(ys::ArrayPartition, g::SeparableSum, xs::ArrayPartition, gamma::Number) = prox!(ys.x, g, xs.x, gamma)
+prox!(ys::ArrayPartition, g::SeparableSum, xs::ArrayPartition, gammas::Tuple) = prox!(ys.x, g, xs.x, gammas)
+function prox(g::SeparableSum, xs::ArrayPartition, gamma=1)
+    y, fy = prox(g, xs.x, gamma)
+    return ArrayPartition(y), fy
+end
+gradient!(grads::ArrayPartition, g::SeparableSum, xs::ArrayPartition) = gradient!(grads.x, g, xs.x)
+gradient(g::SeparableSum, xs::ArrayPartition) = gradient(g, xs.x)
